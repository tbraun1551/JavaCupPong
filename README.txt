To run the program, unzip the folder and open terminal. 

To compile and run the program cd into the directory through java and enter the command:
Java *.java && java Main

To play the game, follow the instructions on screen. It utilizes the users spacebar and number keys. 